# DEBUG TILL DAWN

A browser game built with [Phaser 3](https://phaser.io/) + TypeScript + Vite.

## Dev

```bash
npm install
npm run dev      # start dev server
npm run build    # type-check + production build
npm run preview  # preview the build
```
